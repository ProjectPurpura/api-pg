meta {
  name: add_pagamento
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/pagamento
  body: json
  auth: inherit
}

body:json {
  {
    "idPedido": 23,
    "valorPago": 120.5,
    "tipo": "pix"
  }
}

settings {
  encodeUrl: true
}
